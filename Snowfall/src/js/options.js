const options = {
  fonts: [16, 18, 20, 24],
  maxWidth: window.outerWidth,
  maxHeight: window.outerHeight,
  limit: 150,
  flakes: ['&#10052;', '&#10053;', '&#10054;'],
  speeds: [2, 4, 6, 8, 10],
}

export default options;